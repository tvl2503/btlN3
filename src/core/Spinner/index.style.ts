import { ActivityIndicator } from 'react-native';
import styled from 'styled-components/native';
import { SpinnerProps } from './index.types';

export const SpinnerComposedComponent = styled(ActivityIndicator)<SpinnerProps>``;
