import styled from "styled-components";
import { View } from 'react-native';

export const ProgressStarWrapper = styled(View)`

`
export const ItemWrapper = styled(View)`
    flexDirection : row;
    marginVertical : 6px;
    alignItems: center;
    justifyContent : space-between;
`