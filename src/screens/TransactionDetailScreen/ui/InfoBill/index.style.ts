import styled from "styled-components/native";
import Card from "../../../../core/Card";
import Row from "../../../../core/Row";

export const ContainerInfoBill = styled(Card)`
  border-radius: 0;
`;

export const RowCenter = styled(Row)`
  align-items: center;
  justify-content: space-between;
`;