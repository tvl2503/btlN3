import React from 'react';
import SafeArea from '../../core/SafeArea';
import TransactionTab from './ui/TransactionTab';

const TransactionScreen = () => {
  return (
    <SafeArea>
      <TransactionTab/>
    </SafeArea>
  )
};

export default TransactionScreen;
