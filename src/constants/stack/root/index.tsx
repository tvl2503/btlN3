import CheckoutStack from '../../../navigators/CheckoutStack';
import { NAVIGATION } from '../../navigation';

export const ROOT_STACK_ELEMENTS: Array<any> = [
  {
    name: NAVIGATION.CHECKOUT_HOME,
    component: CheckoutStack
  },
];
